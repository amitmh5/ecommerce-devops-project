apiVersion: v1
kind: Service

metadata:
  name: backend-service
  namespace: ecommerce

spec:
  type: NodePort

  selector:
    app: backend

  ports:
  - port: 8081
    targetPort: 8081
    nodePort: 30080
